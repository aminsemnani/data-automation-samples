# Portfolio Notes

The repository now covers five complementary workflow types: spreadsheet cleanup, JSON validation and transformation, Excel management reporting, SQL reconciliation, and REST API synchronization.

The examples are intentionally independent so a reviewer can inspect a relevant pattern without needing to understand a larger application first.
